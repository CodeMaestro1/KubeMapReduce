FROM golang:1.26-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -trimpath -ldflags="-s -w" -o /api ./manager-service/cmd/api

FROM busybox:1.37
COPY --from=builder /api /api
EXPOSE 8081
ENTRYPOINT ["/api"]
